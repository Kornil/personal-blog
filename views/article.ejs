<!DOCTYPE html>
<html lang="en">
  <head>
    
	<% include ./partials/head %>

  </head>

  <body>

    <% include ./partials/nav %>
    <div class="container">
      <div class="row">
        <div class="col col-md-8 article">

          <h1 id="title"><%= article.title %></h1>
          <h4 id="log-subtitle">// <%= article.subtitle %></h4>
          <h5> { author: "<span class="bolder"><%= article.author %></span>", date: "<span class="bolder"><%= article.date %></span>" } </h5>
          <img class="article-image" src="<%= article.image %>" />
          <div id="article-text"></div>       
          <h5 id="log-end">/* end article */</h5>

        </div>
      </div>
    </div>

    <script>
    $(document).ready(function(){

      $("#article-text").html(
        marked( <%- JSON.stringify({content: article.text}) %>.content )
      )

    })

    </script>

  </body>
</html>